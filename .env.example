# PROVENIQ — REQUIRED SERVER CONFIG (TEMPLATE)
# This file is non-secret. Copy to .env.local (or .env) and fill values.

# --- Environment ---
PROVENIQ_ENV=dev

# --- Database (Supabase Postgres) ---
# Use pooled connection for runtime if needed; use direct for migrations.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME
DIRECT_DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME

# --- Firebase Admin (Identity Provider) ---
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# IMPORTANT: keep quotes; newline escapes may be required depending on runtime.
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# --- Service Config ---
PORT=3001

# --- Origins-specific ---
# Google Drive OAuth (for cloud backup)
GOOGLE_DRIVE_CLIENT_ID=
GOOGLE_DRIVE_CLIENT_SECRET=
GOOGLE_AUTH_REDIRECT_URI=http://localhost:3001/api/auth/google/callback
GOOGLE_DRIVE_REDIRECT_URI=http://localhost:3001/api/cloud/google/callback

# Dropbox (optional)
DROPBOX_APP_KEY=
DROPBOX_APP_SECRET=
DROPBOX_REDIRECT_URI=http://localhost:3001/api/cloud/dropbox/callback

# OpenAI (for Noah AI features)
OPENAI_API_KEY=

# Spotify (for song search autocomplete)
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# PROVENIQ Ecosystem Services
LEDGER_API_URL=http://localhost:8006/api/v1
CORE_SERVICE_URL=http://localhost:8000
